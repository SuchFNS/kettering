README.txt

The program will run a game desk using threads for the desk management and game tasks.

In order to execute the program, the following steps should be followed.
    1. Unzip all files to a folder and navigate to the folder.
    2. Enter command `gcc semaphores.c -lpthread` in the command line.
    3. Run executable with `./a.out`.